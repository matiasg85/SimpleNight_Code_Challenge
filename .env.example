# ---------------------------------------------------------------------------
# Copy this file to .env and set values for your local run.
# .env is git-ignored — never commit secrets.
# ---------------------------------------------------------------------------

# Which environment to test against: staging | production  (default: staging)
TEST_ENV=staging

# Override the base URL for any environment (optional).
# When set, this takes precedence over the URL defined in config/environments.ts.
# BASE_URL=https://wl.stg.simplenight.com
